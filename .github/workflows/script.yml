name: "Script"

on:
  workflow_dispatch:


jobs:
  job1: 

    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run shasum
        run: |
          shasum -a 256 file2

      - name: Check jq
        run: |
         jq --version
